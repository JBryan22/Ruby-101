START

PRINT welcoming message and ask for two numbers

GET number 1
GET number 2

add number1 and number2 together

PRINT result

END

START

define the method with one parameter that is an array

  combine the array into one string
  return the new concat string

END

START

define method(array of ints)

  return if array is nil
  SET newarray = nil

  while iterator < length of array
    newarray += array[iterator]
    iterator + 2
  end

  return newarray

END
